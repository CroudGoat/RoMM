## 1. 理論的課題・改善点 (Theoretical Issues)

### Issue 1.3: ノルム逆数補正におけるノイズ増幅リスク
* **現状の欠陥:**
  * 式: $w_A \propto \frac{1}{\|v_A\|_2}$、および Densityにおける $\frac{\overline{\|v\|}}{\|v_i\|_2}$
  * ある層でモデルの学習変化量が微小（$\|v\| \approx 0$）である場合、それは「ベースモデルから変更する必要がなかった非重要層」である可能性が高い。
  * その層に対して逆数スケーリングをかけると、学習ノイズや微小ドリフトが最大重みでモデルに焼き付けられ、生成崩壊のトリガーとなる。
* **改善提案:**
  * 正則化項 $\epsilon$ の導入、およびベースモデルからの変化量が一定値以下の場合は重みブーストを行わずベース重みを維持する「Dead-zone（不感帯）」の設計。

---

### Issue 1.4: SLERP補間係数 $t$ における方向反転バグとノルム依存の理論的矛盾
* **現状の欠陥:**
  * 式: $t_{\mathrm{raw}}^{(l)} = \frac{\|v_A^{(l)}\|_2}{\|v_A^{(l)}\|_2 + \|v_B^{(l)}\|_2}$
  * MergekitのSLERP仕様では $t=0$ がモデルA、$t=1$ がモデルBに対応する。
  * この定義では、モデルAの学習変化量（$\|v_A\|$）がモデルBより著しく大きい場合、$t_{\mathrm{raw}} \to 1.0$ となり、マージ結果がモデルB（クリップ上限 $t=0.80$）に強く引きずられる。
  * 「ノルムが大きいモデルの学習成果を反映させたい」という意図であるならば、数式の分子分母が逆転している致命的なバグである。
  * 仮に「両者を釣り合わせるための逆数補正」という意図だとしても、SLERP（超球面上の角度補間）の補間係数に対して、ベクトルのノルム比を線形に割り当てる幾何学的・統計学的根拠が存在しない。
* **改善提案:**
  * 「ノルム優位モデルの知識継承」を意図する場合、分子をモデルBのノルムに入れ替え、$t_{\mathrm{raw}}^{(l)} = \frac{\|v_B^{(l)}\|_2}{\|v_A^{(l)}\|_2 + \|v_B^{(l)}\|_2}$ とする（$\|v_A\|$ が大きいほど $t \to 0$ でモデルAに接近）。
  * または、SLERPの幾何学的純粋性を保つため、補間係数は原則 $t = 0.5$（大円上の角の二等分線）に固定し、ノルムの差異はマージ後のテンソルに対するマグニチュード・スケーリングとして分離処理する。

  ---

### Issue 1.5: 低ランク実効次元の無視による $Z$ スコア過大インフレと閾値の恣意性
* **現状の欠陥:**
  * 式: $\sigma_D = \frac{1}{\sqrt{D_l}}, \quad Z_l = Score_l \cdot \sqrt{D_l}$
  * 中心極限定理に基づくノイズフロア $\sigma_D$ の算出は、「パラメータの全要素が互いに独立同分布（i.i.d.）」であることを前提としている。
  * しかし、LLMのファインチューニングにおける重み空間は著しい低ランク性（Low Intrinsic Dimension）を有しており、実効自由度 $D_{\mathrm{eff}}$ は物理次元 $D \approx 10^7$ より数桁小さい（$10^2 \sim 10^4$ オーダー）。
  * 物理次元数 $D_l$ をそのまま乗算することで $Z$ スコアが異常に過大評価（インフレ）され、通常の統計的棄却域（$\pm 3\sigma$）が機能不全に陥っている。
  * その結果、正側の閾値として $+15.0\sigma$ という天文学的に歪な数値を人為的に設定せざるを得なくなっており、「統計的根拠に基づく選定」という本手法の根幹が形骸化している。
* **改善提案:**
  * 特異値分解（SVD）のエントロピーやParticipation Ratioを用いて層ごとの実効次元 $D_{\mathrm{eff}}$ を推定し、$\sqrt{D_{\mathrm{eff}}}$ で正規化を行う。
  * または、絶対的な $Z$ スコアではなく、全レイヤーにおける類似度分布の分位点（パーセンタイル）に基づく適応的（Adaptive）なしきい値判定を導入する。

---

### Issue 1.6: レイヤー間（Depth-wise）におけるマージ手法の不連続性とResidual Streamの破壊
* **現状の欠陥:**
  * レイヤーごとに独立してルーティングを行うため、隣接する層間で「SLERP（全保持補間）」と「強スパースDARE-TIES（95%間引き）」がパッチワーク状に激しく入れ替わる現象が発生する。
  * Transformerは残差接続（Residual Stream）を介して深さ方向に表現を累積するアーキテクチャであるため、層ごとに急激なスパース率やノルムスケールの断絶が生じると、隠れ状態（Hidden States）の内部表現分布が崩壊し、異常出力やハルシネーションの要因となる。
* **改善提案:**
  * レイヤーごとのルーティング判定値（$Z$ スコアおよびDensity/Weight）に対し、層方向（Depth-wise）の移動平均（Moving Average）やガウシアンフィルタによる平滑化（Smoothing）を導入し、隣接層との間でマージ特性が滑らかに遷移する機構を設ける。
---


## 2. 実装的課題・改善点 (Implementation Issues)

### Issue 2.1: レイヤー全体の集約によるサブモジュール幾何情報の不可視化
* **現状の欠陥:**
  * `stream_tensor_stats` において、Attention（Q, K, V, O）と MLP（Gate, Up, Down）の全テンソルを同一レイヤー内で一括して内積・ノルム合算している。
  * 実際には「Attentionは高相関（SLERP向き）、MLPは直交（DARE-TIES向き）」という相反する特徴が同一層内で頻出するが、合算によって相殺され、正確な幾何的特徴がマスクされている。
* **改善提案:**
  * テンソル名に基づくモジュール分離（`attn` vs `mlp`）を行い、モジュールごとのコサイン類似度およびノルムを個別算出する。
  * Mergekit YAML生成時にも `parameters` 配下の `filter` 構文を活用し、サブモジュール単位で異なるパラメータを注入する。

---

### Issue 2.2: 非Transformer層（Embedding, Final Norm, lm_head）の脱落
* **現状の欠陥:**
  * `DEFAULT_LAYER_PATTERN = r"(?:^|\.)layers\.(\d+)(?:\.|$)"` でマッチしたTransformerブロックのみを処理し、`slices` に出力している。
  * `embed_tokens` や `lm_head`、最終LayerNormなどの非スライス層がYAMLに記述されず、Mergekit側でデフォルトのベースモデル重みがそのまま採用されるか、不整合を起こす。
  * 特に追加事前学習や日本語チューニングでトークン埋め込み層を更新している場合、その学習成果が100%消失する致命的なバグとなる。
* **改善提案:**
  * 非レイヤーテンソルを検出し、ベースモデルと派生モデル間の差分が存在する場合は、明示的なマージルール（通常は安全なSLERPまたは加重平均）をYAMLのトップレベルや補足スライスに付与する。

---

### Issue 2.3: ゼロ除算・極小値に対するロバストネスの不足
* **現状の欠陥:**
  * `mean_norm / norm_a if norm_a > 0 else 1.0`
  * 浮動小数点のアンダーフローや極小値（例: `1e-9`）のとき、`norm_a > 0` の判定を通過して天文学的な倍率が発生し、直後の `min(..., 0.50)` で強制的に上限張り付きを起こす。
* **改善提案:**
  * `norm_a > 1e-6` のような明確なイプシロン閾値を設けるか、`torch.clamp` 的な事前正規化を挟む。

---

### Issue 2.4: 幾何構造の可視化および診断レポート機能の欠如
* **現状の欠陥:**
  * 出力がCSV、JSON、Mergekit YAMLのみであり、ユーザーは「なぜこの層がSLERPになったのか」「どのモジュールで干渉が起きているのか」を直感的に把握できない。
* **改善提案:**
  * レイヤーごとのコサイン類似度と各モデルのタスクベクトルノルムを一覧表示するSVG/PNGヒートマップの自動生成機能を追加。

---

### Issue 2.5: Mergekit依存と「テンソル単位マージ」の不可能化
* **現状の欠陥:**
  * Mergekitの単一YAML仕様（`slices`）に依存しているため、1つのレイヤー内でAttentionをSLERP、MLPをDARE-TIESにするようなハイブリッドマージが実行できない。
* **改善提案:**
  * 将来的なマイルストーンとして、YAMLの生成にとどまらず、PyTorch上で直接 `safetensors` をストリーミングマージして完成モデルを出力する `romm-engine` の自作内蔵を計画する。