## 背景
モデルマージツールのデファクトスタンダードになっているMergekitが一般ユーザーに推奨されるのは、mergekitによって可能なSLERPやTIESなどのマージ手法は、そのほとんどがtrain-free(学習不必要)であり、非常に高速かつ軽量なためである。これによって、Google ColabのCPUや、VRAMが潤沢ではないユーザーでも簡単に、直感的にモデルマージが可能になり、それによってモデルマージコミュニティも広大に発展した。 しかし、それらのマージ手法・マージパラメータの選定はユーザーに依存しており、経験的な勘で試行錯誤のマージをしている側面もある。かといって、EvoLLMなどのマージパラメータを自動探索する試みは、ベンチマークの実行や複数モデルの推論など、限られたリソースを持つ一般ユーザーにとっては厳しいものである。
そのため、このドキュメントでは、パラメータの幾何学的特徴に基づいて、レイヤーごとに適切なマージ手法・マージパラメータの選定を行う、**Routing of Model Merging (RoMM)**を提唱する。幾何学特徴によるマージ手法の選定・マージパラメータの選定をRouting of Model Mergingとして体系化し、実用化することが、RoMMの目的である。

## 手法の詳細

### 1. マージされる同士のパラメータ特徴抽出
二つのモデルをマージする際、それぞれをモデルAとモデルBとし、これらは同一モデルからファインチューニングされたモデルであり、同一アーキテクチャを使用しているとする。モデルAとモデルBの1レイヤーブロックごとのベースモデルに対するタスクベクトルの特徴を、以下の二点抽出する。

- モデルAとモデルBのコサイン類似度
- モデルAとモデルBのノルム

このドキュメントにおいて、レイヤーLのタスクベクトルは以下のように定義する。
$$\Delta_A^{(l)} = W_A^{(l)} - W_0^{(l)}$$
$$\Delta_B^{(l)} = W_B^{(l)} - W_0^{(l)}$$
コサイン類似度を解析する際、レイヤーブロック内の各サブモジュール群を単一ベクトルにflatten・連結し、モデルA、モデルBのそれぞれのレイヤーベクトルを作成し、コサイン類似度(類似度スコア)を求める。
$$v_A^{(l)} = [\operatorname{vec}(\Delta_q), \operatorname{vec}(\Delta_k), \dots]$$
$$v_B^{(l)} = [\operatorname{vec}(\Delta_q), \operatorname{vec}(\Delta_k), \dots]$$
$$Score_{l} = \cos(v_A^{(l)}, v_B^{(l)})$$

### 2. マージ方法の決定
各レイヤー同士のコサイン類似度によって、適切なマージ方法を決定する。

#### レイヤー同士が類似している場合
cos ≈ 1など、レイヤー同士が類似している場合、SLERPマージを行う。既存の理論上、レイヤー同士が類似しているという事は、パラメータのマクロ方向性が同じであり、符号衝突が非常に少ないため、SLERPによって滑らかに補完するのが最も安定する。

#### レイヤー同士が直交である場合
このドキュメントの理論では、cos ≈ 0など、レイヤー同士が直交である場合、DARE-TIESマージを行う。SLERPはパラメータ補間であるため、能力が希釈したりノルムの大小によって片方のモデルの能力が無視されてしまう可能性があるが、DARE-TIESにおいては、直交しているという事はマクロでの競合が無く、局所的な衝突するミクロパラメータが存在するのみであるため、各パラメータを失わずに能力を適切にマージする事が出来る。

#### レイヤー同士が衝突している場合
cos ≈ -1など、レイヤー同士が衝突している場合、パラメータ補間による破壊的マージを防ぐため、DARE-TIESによってパラメータをスパース化させ、パラメータ競合を防ぐ

>今回のドキュメントでは、-0.2 <= score <=0.2を「レイヤー同士が直交である」と定義する。

### 3. マージパラメータの決定
レイヤー同士のコサイン類似度とそれぞれのベクトルのノルムの大小によって決定する。

#### SLERPのマージパラメータ決定
モデルAを一つ目のモデル、モデルBを二つ目のモデルとした時の各レイヤー$l$の補間係数$t$は次の式で決定する
$$t^{(l)} = \frac{\|v_A^{(l)}\|_2}{\|v_A^{(l)}\|_2 + \|v_B^{(l)}\|_2}$$
両者の影響力が釣り合うように、ノルムの大きさに配慮した公平な補間係数を決定する。
Mergekit などの実装では、`models`の1番目が$t=0$、2番目が$t=1$に対応する。

#### DARE-TIESのマージパラメータ決定

##### Density parameter
モデル$i$のレイヤー$l$をマージする際のDARE-TIES Densityパラメータは、次の式で決定する
$$d_i^{(l)} = \text{clip}\left( D_{\text{base}} \times \underbrace{\left( 1 + |Score_l| \right)}_{\text{① 直交度補正}} \times \underbrace{\left( \frac{\overline{\|v\|}}{\|v_i^{(l)}\|_2} \right)}_{\text{② ノルム補正}}, \quad \underbrace{0.05}_{\text{下限}}, \quad \underbrace{0.50}_{\text{上限}} \right)$$

- $D_{\text{base}}$: 基準密度（デフォルト値。通常 `0.2`＝残存率20%)
- $Score_l$: レイヤー $l$ のコサイン類似度（$-1 \le Score_l \le 1$）
- $\|v_i^{(l)}\|_2$: モデル $i$ のレイヤー $l$ における実測タスクベクトルノルム 
- $\overline{\|v\|}$: マージに使用される全層・全モデルのタスクベクトルノルムの相加平均（基準スケール）

直交度が高い、相関度が低い場合、Density補正係数はデフォルトの1.0に近くなり、scoreが-1、1に近く、直交度が低い場合、Density補正係数が最大2.0に強化される。
直交度が高いという事は、本質的タスクベクトル同士が別々の空間に存在し、Densityを低めに設定してもdropによって能力を失う・リスケール後に衝突して能力が消えるなどの可能性が低く、直交度が低いという事は、本質的タスクベクトルが衝突しており、Densityを低めに設定してしまうと、衝突した時の能力損失が非常に大きくなるため、安全策としてdensityを上げる。

#### Weight parameter
対等なweightを決定することが目的。ノルムの逆数の比で決定する。$\alpha_A : \alpha_B$（基本は $1 : 1$）とした場合、 
$$w_A^{(l)} = \frac{\alpha_A \|v_B^{(l)}\|_2}{\alpha_A \|v_B^{(l)}\|_2 + \alpha_B \|v_A^{(l)}\|_2}, \quad w_B^{(l)} = \frac{\alpha_B \|v_A^{(l)}\|_2}{\alpha_A \|v_B^{(l)}\|_2 + \alpha_B \|v_A^{(l)}\|_2}$$
によって、ノルムが大きいほどweightが小さくなり、ノルムが小さいほどweightが大きくなる。