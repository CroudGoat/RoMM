## 背景
モデルマージツールのデファクトスタンダードになっているMergekitが一般ユーザーに推奨されるのは、mergekitによって可能なSLERPやTIESなどのマージ手法は、そのほとんどがtrain-free(学習不必要)であり、非常に高速かつ軽量なためである。これによって、Google ColabのCPUや、VRAMが潤沢ではないユーザーでも簡単に、直感的にモデルマージが可能になり、それによってモデルマージコミュニティも広大に発展した。 しかし、それらのマージ手法・マージパラメータの選定はユーザーに依存しており、経験的な勘で試行錯誤のマージをしている側面もある。かといって、EvoLLMなどのマージパラメータを自動探索する試みは、ベンチマークの実行や複数モデルの推論など、限られたリソースを持つ一般ユーザーにとっては厳しいものである。
そのため、このドキュメントでは、パラメータの幾何学的特徴に基づいて、レイヤーごとに適切なマージ手法・マージパラメータの選定を行う、**Routing of Model Merging (RoMM)**を提唱する。幾何学特徴によるマージ手法の選定・マージパラメータの選定をRouting of Model Mergingとして体系化し、実用化することが、RoMMの目的である。

## 手法の詳細

### 1. マージされる同士のパラメータ特徴抽出
二つのモデルをマージする際、それぞれをモデルAとモデルBとし、これらは同一モデルからファインチューニングされたモデルであり、同一アーキテクチャを使用しているとする。モデルAとモデルBの1レイヤーブロックごとのベースモデルに対するタスクベクトルの特徴を、以下の二点抽出する。

- モデルAとモデルBのコサイン類似度
- モデルAとモデルBのノルム

このドキュメントにおいて、レイヤー $l$ のタスクベクトルは以下のように定義する。
$$\Delta_A^{(l)} = W_A^{(l)} - W_0^{(l)}$$
$$\Delta_B^{(l)} = W_B^{(l)} - W_0^{(l)}$$
コサイン類似度を解析する際、レイヤーブロック内の各サブモジュール群を単一ベクトルにflatten・連結し、モデルA、モデルBのそれぞれのレイヤーベクトルを作成し、コサイン類似度(類似度スコア)を求める。
$$v_A^{(l)} = [\mathrm{vec}(\Delta_q), \mathrm{vec}(\Delta_k), \dots]$$
$$v_B^{(l)} = [\mathrm{vec}(\Delta_q), \mathrm{vec}(\Delta_k), \dots]$$
$$Score_{l} = \cos(v_A^{(l)}, v_B^{(l)}) = \frac{v_A^{(l)} \cdot v_B^{(l)}}{\|v_A^{(l)}\|_2 \|v_B^{(l)}\|_2}$$

#### 高次元幾何における中心極限定理と統計的スケーリング ($Z$スコア)
LLMの重み空間のような超高次元空間（$D \approx 10^7 \sim 10^8$）では、中心極限定理により、独立な2つのランダム単位ベクトルの内積の分散は $1/D$ に圧縮される。したがって、偶発的なノイズフロアとなる帰無分布の標準偏差は以下のように表される。
$$\sigma_D = \frac{1}{\sqrt{D_l}}$$
このため、固定閾値による判定は次元数 $D$ の圧縮効果を無視しており機能しない。幾何学的判定には、次元数で正規化した統計的 $Z$ スコアを用いる。
$$Z_l = \frac{Score_l}{\sigma_D} = Score_l \cdot \sqrt{D_l}$$

### 2. マージ方法の決定
各レイヤー同士の統計的 $Z$ スコアによって、幾何構造を3領域に動的分類し、適切なマージ方法を決定する。

#### レイヤー同士が類似している場合（High-Similarity）
$Z_l \ge +15.0\sigma$ のように、統計的有意に類似している場合、SLERPマージを行う。既存の理論上、レイヤー同士が強く類似しているということは、パラメータのマクロ方向性が整合しており、符号衝突が極めて少ないため、SLERPによって大円補間を行うのが最も表現を損なわず安定する。

#### レイヤー同士が直交・緩相関である場合（Orthogonal / Moderate）
$-3.0\sigma \le Z_l < +15.0\sigma$ の領域では、弱スパース DARE-TIESマージを行う。この領域のタスクベクトルは互いに干渉せず、独立した部分空間で機能している。SLERPのような全体補間を行うと表現の希釈を招くが、DARE-TIESを用いることで、非干渉なパラメータを保持しつつ双方の獲得能力を適切に統合できる。

#### レイヤー同士が衝突している場合（Conflicting）
$Z_l < -3.0\sigma$ のように、負の相関（統計的有意な衝突）を示す場合、強スパース DARE-TIESマージを行う。パラメータ補間による相殺破壊を防ぐため、衝突の激しさに応じてパラメータを積極的に間引き（スパース化）、重複競合を物理的に回避する。

### 3. マージパラメータの決定
レイヤー同士のコサイン類似度（$Z$ スコア）とそれぞれのベクトルのノルムの大小によって決定する。

#### SLERPのマージパラメータ決定
モデルAを一つ目のモデル、モデルBを二つ目のモデルとした時の各レイヤー $l$ の補間係数 $t$ は次の式で決定する。
$t_{\text{raw}}^{(l)} = \frac{\|v_A^{(l)}\|_2}{\|v_A^{(l)}\|_2 + \|v_B^{(l)}\|_2}$
両者の影響力が釣り合うように、ノルムの大きさに配慮した公平な補間係数を決定する。ただし、極小ノルム層や外れ値による片肺飛行を防ぐため、ガードレール $[0.20, 0.80]$ を適用する（ゼロ除算時は $t = 0.50$）。
$t^{(l)} = \text{clip}\left( t_{\text{raw}}^{(l)}, \quad 0.20, \quad 0.80 \right)$
※ Mergekit などの実装では、`models`の1番目が$t=0$、2番目が$t=1$に対応する。

#### DARE-TIESのマージパラメータ決定
パラメータの責務を厳密に分離し、**Densityは幾何学的衝突確率の制御**、**Weightはノルムエネルギーの制御**を担う。

##### Density parameter
モデルのレイヤー $l$ をマージする際のDARE-TIES Densityパラメータは、衝突の激しさ（severity）に対する線形ペナルティ関数として決定する。

$$
\begin{aligned}
\mathrm{severity}_l &= \max(0.0, -Z_l) \\
\mathrm{penalty}_l &= \max\left(0.25, \quad 1.0 - 0.75 \times \frac{\mathrm{severity}_l}{15.0}\right) \\
d^{(l)} &= \mathrm{clip}\left( D_{\mathrm{base}} \times \mathrm{penalty}_l, \quad 0.05, \quad 0.50 \right)
\end{aligned}
$$

- $D_{\mathrm{base}}$: 基準密度（デフォルト値。通常 `0.20`＝残存率20%）
- $Z_l$: レイヤー $l$ の統計的 $Z$ スコア
- クリップ範囲 $[0.05, 0.50]$: 下限 $0.05$（過剰スパース防止）、上限 $0.50$（過剰密度防止）

直交〜正相関領域（$Z_l \ge 0$）ではペナルティは発生せず、基準密度 $D_{\text{base}}$ が維持される。一方、衝突（$Z_l < 0$）が激しくなるほどペナルティが強まり、Densityを削ってスパース性を高める。これにより、対立するパラメータ同士の相殺事故を確率論的に最小化する。

##### Weight parameter
対等な重み付けを決定することが目的。ノルムの逆数の比で決定する。ユーザー優先度を $\alpha_A : \alpha_B$（基本は $1 : 1$）とした場合、
$$w_{A,\text{raw}}^{(l)} = \frac{\alpha_A \|v_B^{(l)}\|_2}{\alpha_A \|v_B^{(l)}\|_2 + \alpha_B \|v_A^{(l)}\|_2}$$
ノルムが大きいほどweightが小さくなり、ノルムが小さいほどweightが大きくなる。ただし、非学習層（$\|v\| \approx 0$）による学習ノイズの過剰増幅を防ぐため、ガードレール $[0.15, 0.85]$ を適用する（ゼロ除算時は $w_A = 0.50$）。
$$w_A^{(l)} = \text{clip}\left( w_{A,\text{raw}}^{(l)}, \quad 0.15, \quad 0.85 \right), \quad w_B^{(l)} = 1.0 - w_A^{(l)}$$